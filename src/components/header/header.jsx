import styles from "../header/header.module.css";

function Header() {

    return (
        <header className={styles.header}>
            Kanban Board
        </header>
    );
}

export default Header;
